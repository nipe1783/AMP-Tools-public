#include "AMPCore.h"

class Link {
    public:
        // constructors:
        Link(float l, float a);
        ~Link();

        // methods:
        void hereIsAMethod();

        // fields:
        float length;
        float angle;
}